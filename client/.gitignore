node_modules/
dist/

# Redundant with the root .gitignore (which matches at every depth), but
# explicit here too since this is exactly where VITE_API_URL lives.
.env
.env.local
.env.development
.env.development.local
.env.production
.env.production.local
.env.test
.env.test.local

.vercel

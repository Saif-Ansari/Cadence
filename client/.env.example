# Copy this file to .env.production (or set in Vercel's dashboard) before deploying.
# Not needed for local dev — vite.config.ts proxies /api to localhost:5000 instead.

# The deployed backend's base URL, including the /api prefix.
VITE_API_URL=https://your-backend.up.railway.app/api
